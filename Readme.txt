First install the latest version of Wordpress. 

Make sure we apache user has permissions to read, write and delete to the Wordpress root folder and all its directories. 


To install collab:

- unzip the plugin folder into /wp-content/plugins/
- unzip the theme folder into /wp-content/themes/

Activate both the plugin and the theme within Wordpress.